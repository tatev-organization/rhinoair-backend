model CardInfo {
  cardInfoId String   @id @default(uuid())
  cardNumber String
  cvc        String
  expiryDate String
  userId     String   @unique
  user       User     @relation(fields: [userId], references: [userId], onDelete: Cascade)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
