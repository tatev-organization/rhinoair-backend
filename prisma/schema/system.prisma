model System {
  systemId               String   @id @default(uuid())
  supportEmail          String
  supportUrl            String
  siteName              String
  freeTrialDuration     Int?
  planLimitMaxBusiness  Int?
  planLimitMaxLocations Int?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
}
